import { HOME_KEYS } from '../keys';

export const HOME = {
  [HOME_KEYS.HOME_API_ROOM_NAME]: 'Sorry~ Room name already exists!',
  [HOME_KEYS.HOME_API_NO_ROOM_NAME]: 'Sorry ~ Room name not exists!',
  [HOME_KEYS.HOME_API_PASSWORD_ERROR]: 'Password error',

  [HOME_KEYS.AVATAR]: 'Avatar',
} as const;
